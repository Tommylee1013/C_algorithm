//
// Created by Tommy Lee on 7/11/23.
//
#include <stdio.h>
#include <stdlib.h>

/*
int main(void) {
    int i;
    int *a;
    int na;
    printf("Number of elements : "); scanf("%d", &na);
    a = calloc(na, sizeof(int));

    if (a == NULL) puts("fail to make memories.");
    else {
        printf("Enter the number of %d integers.\n", na);
        for (i = 0; i < na; i++) {
            printf("a[%d] : ", i);
            scanf("%d", & a[i]);
        }
    }
    printf("The values of each element are as follows \n");
    for (i = 0; i < na; i++) printf("a[%d] = %d\n", i, a[i]);
    free(a);

    return 0;
}
*/